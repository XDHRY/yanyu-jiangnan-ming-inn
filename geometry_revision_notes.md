# Geometry revision notes

- `build_scene.py`: roof slope/overhang tightened, continuous wall plate added, gable base aligned.
- `build_scene.py`: `tree_cluster()` now uses a tapered trunk, primary/secondary branches, and layered leaf clusters with clear negative space.
- `refine_ming.py` and `make_review.py` were rerun after the revision.
- Function contract validation: `PASS: function gate`.
- Form and Runtime are intentionally not asserted: native Blender/Cycles execution and browser capture remain separate gates.
